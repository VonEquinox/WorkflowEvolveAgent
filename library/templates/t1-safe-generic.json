{
  "id": "t1-safe-generic",
  "version": "1.0.0",
  "summary": "T1 SafeGeneric: Inspect -> Implement -> Verify with one bounded fix loop (max 2 passes).",
  "graph": {
    "nodes": [
      {
        "id": "inspect",
        "kind": "planner",
        "agentCard": "inspector",
        "trigger": "ALL_SUCCESS",
        "promptTemplate": "Task:\n${task}\n\nInspect the working directory and produce the plan JSON."
      },
      {
        "id": "implement",
        "kind": "worker",
        "agentCard": "implementer",
        "trigger": "ALL_SUCCESS",
        "promptTemplate": "Task:\n${task}\n\nUpstream (plan, and verifier feedback if this is a fix pass):\n${upstream}\n\nImplement (or fix) the change and produce the JSON report. If the upstream contains verifier must_fix items, address exactly those."
      },
      {
        "id": "verify",
        "kind": "verifier",
        "agentCard": "verifier",
        "trigger": "ALL_SUCCESS",
        "promptTemplate": "Task:\n${task}\n\nImplementer report:\n${upstream}\n\nIndependently verify and produce the verdict JSON."
      }
    ],
    "edges": [
      { "id": "e_in_inspect", "from": "@input", "to": "inspect", "kind": "DATA" },
      { "id": "e_inspect_impl", "from": "inspect", "to": "implement", "kind": "DATA" },
      { "id": "e_impl_verify", "from": "implement", "to": "verify", "kind": "DATA" },
      { "id": "e_verify_out", "from": "verify", "to": "@output", "kind": "DATA" },
      { "id": "e_fix_loop", "from": "verify", "to": "implement", "kind": "FEEDBACK", "loopId": "fix" }
    ],
    "loops": [
      { "id": "fix", "bodyNodes": ["implement", "verify"], "feedbackEdges": ["e_fix_loop"], "maxIterations": 2 }
    ]
  }
}
